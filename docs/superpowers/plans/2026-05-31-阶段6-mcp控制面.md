# 阶段 6 MCP 控制面 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Claude Code/Codex 经 MCP 操控工作台（编排/运行/看产物/造节点），并在画布上实时看过程。

**Architecture:** 抽无 Qt 执行核 `WorkflowRunner`（GUI 与 MCP 共用）；`app/mcp/store.py` 以 `workflows/active.json`+`rev` 为单一真相源做编排/运行纯函数；`app/mcp/server.py` 用 FastMCP stdio 暴露工具；GUI 改走 Runner 并用 `QFileSystemWatcher` 监听 active.json 重绘。

**Tech Stack:** Python 3.13、PyQt5、mcp（FastMCP, stdio）、既有 ToolExecutor / node_builder。

**Spec:** docs/superpowers/specs/2026-05-31-阶段6-mcp控制面-design.md

---

## 环境与约定（本机特例）

- 真实解释器：`C:\Users\ASUS\AppData\Local\Programs\Python\Python313\python.exe`。
- 装依赖（按 uv 规则）：`uv pip install mcp`（在工作台目录）。
- 无 pytest，用临时 runner 调 `test_*`（tmp_path 注入临时目录）。模板见
  2026-05-31-移除内置总控与统一凭证.md 顶部。
- GUI 冒烟用 venv：`J:\MagicTool\Standalone\VideoCut\video_tool\venv\Scripts\python.exe` +
  `$env:QT_QPA_PLATFORM="offscreen"`。
- 提交只在用户要求时（每 Task 的 commit 步骤默认暂存不提交）。

## File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `app/runtime/runner.py` | 新建 | `WorkflowRunner`：无 Qt 的 DAG 编排 + 模拟/真实执行 + 进度回调 |
| `app/mcp/__init__.py` | 新建 | 包标记 |
| `app/mcp/store.py` | 新建 | 工作流仓库：active.json 读写 + rev + add/connect/set/delete/run/造节点纯函数 |
| `app/mcp/server.py` | 新建 | FastMCP stdio 薄壳，注册工具调 store |
| `app/runtime/engine.py` | 改 | GUI 适配：用 WorkflowRunner + 回调转 Qt 信号（替换 QTimer/线程编排）|
| `app/ui/main_window.py` | 改 | 跑流程改走 Runner；active.json autosave + QFileSystemWatcher 重载 |
| `.mcp.json` | 新建 | Claude Code 接入配置 |
| `tests/test_runner.py` | 新建 | Runner 编排/执行 |
| `tests/test_mcp_store.py` | 新建 | 仓库读写/编排/rev/确认闸门/造节点 |

---

## Task 1: WorkflowRunner（无 Qt 执行核）

**Files:**
- Create: `app/runtime/runner.py`
- Test: `tests/test_runner.py`

- [ ] **Step 1: 写失败测试**（`tests/test_runner.py`）

```python
"""WorkflowRunner：DAG 编排、模拟/真实分流、产物传递、失败阻断、确认闸门。"""
from __future__ import annotations
from app.models import NODE_SPEC_BY_TYPE, WorkflowNode, WorkflowEdge
from app.runtime.runner import WorkflowRunner


def _node(type_, **params):
    spec = NODE_SPEC_BY_TYPE[type_]
    p = dict(spec.default_params); p.update(params)
    n = WorkflowNode(spec=spec, x=0, y=0)
    n.params = p
    return n


def test_runs_chain_in_dependency_order():
    a = _node("douyin_profile_collect", 执行模式="模拟")
    b = _node("douyin_video_download", 执行模式="模拟")
    c = _node("asr_extract", 执行模式="模拟")
    nodes = {n.id: n for n in (a, b, c)}
    edges = [WorkflowEdge(a.id, b.id), WorkflowEdge(b.id, c.id)]
    seen = []
    r = WorkflowRunner(on_progress=lambda nid, st, _o: seen.append((nid, st)) if st == "success" else None)
    summary = r.run_sync(nodes, edges)
    order = [nid for nid, st in seen]
    assert order == [a.id, b.id, c.id]
    assert set(summary["ran"]) == {a.id, b.id, c.id}
    assert a.last_output and b.last_output and c.last_output


def test_downstream_receives_upstream_output():
    a = _node("douyin_profile_collect", 执行模式="模拟")
    b = _node("douyin_video_download", 执行模式="模拟")
    nodes = {n.id: n for n in (a, b)}
    edges = [WorkflowEdge(a.id, b.id)]
    captured = {}
    r = WorkflowRunner()
    r.run_sync(nodes, edges)
    # b 的输出 meta 记录了 upstream 数量（模拟实现里 upstream_count）
    assert b.last_output["meta"]["upstream_count"] == 1


def test_needs_confirmation_blocks_real_high_risk():
    a = _node("mediapush_publish", 执行模式="真实")  # requires_confirmation=True
    nodes = {a.id: a}
    r = WorkflowRunner()
    summary = r.run_sync(nodes, [])
    assert summary["needs_confirmation"] and summary["needs_confirmation"][0]["id"] == a.id
    assert a.id not in summary["ran"]
    # 带 confirm 放行后可跑（仍是模拟避免真实副作用：改回模拟验证 ran）
    a.params["执行模式"] = "模拟"
    summary2 = r.run_sync(nodes, [], confirm=True)
    assert a.id in summary2["ran"]
```

- [ ] **Step 2: 跑测试确认失败**（runner 不存在）。

- [ ] **Step 3: 实现 `app/runtime/runner.py`**

```python
"""无 Qt 的工作流执行核：GUI 与 MCP 共用。

把原先散在 GUI（main_window._run_chain/_start_ready_nodes）与 engine 的编排逻辑
收敛到这里。模拟模式产出 mock 输出；真实模式调 ToolExecutor。
"""
from __future__ import annotations
from datetime import datetime
from typing import Callable

from app.models import WorkflowNode, WorkflowEdge
from app.runtime.tool_executor import ToolExecutionError, ToolExecutor

ProgressCb = Callable[[str, str, dict | str | None], None]

_MOCK = {
    "douyin_profile_collect": ["https://douyin.com/video/demo-001", "https://douyin.com/video/demo-002"],
    "douyin_video_download": ["outputs/downloads/demo-001.mp4"],
    "asr_extract": ["outputs/scripts/demo-001.txt"],
    "script_rewrite": ["outputs/scripts/batch_tasks.json"],
    "batch_mix": ["outputs/videos/final-001.mp4"],
    "mediapush_publish": ["publish_records/demo-run.json"],
    "article_md_import": ["# 示例文章\n\n正文。"],
    "wechat_article_assemble": ["outputs/articles/assembled-demo.md"],
}


def _mock_items(node: WorkflowNode) -> list[str]:
    return _MOCK.get(node.spec.type, ["outputs/result.json"])


class WorkflowRunner:
    def __init__(self, executor: ToolExecutor | None = None, on_progress: ProgressCb | None = None) -> None:
        self._executor = executor or ToolExecutor()
        self._on_progress = on_progress or (lambda *a: None)

    def run_sync(
        self,
        nodes: dict[str, WorkflowNode],
        edges: list[WorkflowEdge],
        start_ids: list[str] | None = None,
        confirm: bool | list[str] | None = None,
    ) -> dict:
        scope = self._scope(nodes, edges, start_ids)
        for nid in scope:
            nodes[nid].status = "idle"
        summary = {"ran": [], "failed": [], "needs_confirmation": [], "skipped": []}
        completed: set[str] = set()

        def upstream_ready(nid: str) -> bool:
            return all(e.source_id in completed for e in edges if e.target_id == nid and e.source_id in scope)

        remaining = [nid for nid in scope]
        progressed = True
        while remaining and progressed:
            progressed = False
            for nid in list(remaining):
                if not upstream_ready(nid):
                    continue
                node = nodes[nid]
                # 上游若有失败/待确认导致未完成，则该节点阻断
                blocked = any(
                    e.source_id in scope and e.source_id not in completed
                    for e in edges if e.target_id == nid
                )
                if blocked:
                    continue
                remaining.remove(nid)
                progressed = True
                if self._needs_confirm(node, confirm):
                    node.status = "idle"
                    summary["needs_confirmation"].append(
                        {"id": nid, "name": node.spec.name, "risk": node.spec.risk_level}
                    )
                    continue
                self._run_one(node, nodes, edges, summary)
                if node.status == "success":
                    completed.add(nid)
        summary["skipped"] = [nid for nid in remaining]
        return summary

    def _run_one(self, node, nodes, edges, summary) -> None:
        node.status = "running"
        self._on_progress(node.id, "running", None)
        upstream = [nodes[e.source_id].last_output for e in edges
                    if e.target_id == node.id and nodes[e.source_id].last_output]
        try:
            if str(node.params.get("执行模式", "模拟")).strip() == "真实":
                output = self._executor.execute(node, upstream)
            else:
                output = {
                    "type": node.spec.outputs[0] if node.spec.outputs else "generic",
                    "items": _mock_items(node),
                    "meta": {"node_type": node.spec.type, "upstream_count": len(upstream),
                             "finished_at": datetime.now().strftime("%Y-%m-%d %H:%M:%S")},
                }
        except ToolExecutionError as exc:
            node.status = "failed"; node.error = str(exc)
            summary["failed"].append({"id": node.id, "error": str(exc)})
            self._on_progress(node.id, "failed", str(exc)); return
        except Exception as exc:  # noqa: BLE001
            node.status = "failed"; node.error = f"未预期错误: {exc}"
            summary["failed"].append({"id": node.id, "error": node.error})
            self._on_progress(node.id, "failed", node.error); return
        output.setdefault("meta", {})["source_node_id"] = node.id
        node.last_output = output; node.status = "success"; node.error = ""
        summary["ran"].append(node.id)
        self._on_progress(node.id, "success", output)

    @staticmethod
    def _needs_confirm(node: WorkflowNode, confirm) -> bool:
        real = str(node.params.get("执行模式", "模拟")).strip() == "真实"
        if not (real and node.spec.requires_confirmation):
            return False
        if confirm is True:
            return False
        if isinstance(confirm, list) and node.id in confirm:
            return False
        return True

    @staticmethod
    def _scope(nodes, edges, start_ids) -> list[str]:
        if not start_ids:
            return list(nodes.keys())
        scope: set[str] = set()
        adj: dict[str, list[str]] = {}
        for e in edges:
            adj.setdefault(e.source_id, []).append(e.target_id)
        stack = list(start_ids)
        while stack:
            nid = stack.pop()
            if nid in scope or nid not in nodes:
                continue
            scope.add(nid)
            stack.extend(adj.get(nid, []))
        return [nid for nid in nodes if nid in scope]
```

- [ ] **Step 4: 跑测试确认通过**（3 个 test）。
- [ ] **Step 5: （可选）暂存**。

---

## Task 2: 工作流仓库 store — 读写 + rev + 反序列化

**Files:**
- Create: `app/mcp/__init__.py`（空）
- Create: `app/mcp/store.py`
- Test: `tests/test_mcp_store.py`

- [ ] **Step 1: 写失败测试**

```python
"""工作流仓库：active.json 读写、rev 自增、节点反序列化。"""
from __future__ import annotations
from app.mcp import store as S

def test_empty_load(tmp_path):
    wf = S.load_workflow(tmp_path / "active.json")
    assert wf["rev"] == 0 and wf["nodes"] == [] and wf["edges"] == []

def test_save_bumps_rev(tmp_path):
    p = tmp_path / "active.json"
    wf = S.load_workflow(p)
    r1 = S.save_workflow(wf, p)
    r2 = S.save_workflow(S.load_workflow(p), p)
    assert r2 == r1 + 1
```

- [ ] **Step 2: 跑测试确认失败**。

- [ ] **Step 3: 实现 store.py 的读写部分**

```python
"""工作流仓库：以 workflows/active.json 为单一真相源（带 rev）。

供 MCP server 调用；纯函数、无 Qt，可单测。
"""
from __future__ import annotations
import json
from pathlib import Path
from typing import Any

PROJECT_ROOT = Path(__file__).resolve().parents[2]
ACTIVE_PATH = PROJECT_ROOT / "workflows" / "active.json"


def _empty() -> dict[str, Any]:
    return {"version": "0.1.0", "rev": 0, "nodes": [], "edges": []}


def load_workflow(path: Path | None = None) -> dict[str, Any]:
    path = path or ACTIVE_PATH
    if not path.exists():
        return _empty()
    data = json.loads(path.read_text(encoding="utf-8"))
    data.setdefault("rev", 0); data.setdefault("nodes", []); data.setdefault("edges", [])
    return data


def save_workflow(data: dict[str, Any], path: Path | None = None) -> int:
    path = path or ACTIVE_PATH
    path.parent.mkdir(parents=True, exist_ok=True)
    data["rev"] = int(data.get("rev", 0)) + 1
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    return data["rev"]
```

- [ ] **Step 4: 跑测试确认通过**。
- [ ] **Step 5: （可选）暂存**。

---

## Task 3: store 编排操作（add/connect/set/delete）

**Files:** Modify `app/mcp/store.py`；Test 追加 `tests/test_mcp_store.py`

- [ ] **Step 1: 追加失败测试**

```python
def test_add_connect_validates_ports(tmp_path):
    p = tmp_path / "active.json"
    a = S.add_node("douyin_profile_collect", path=p)["id"]
    b = S.add_node("douyin_video_download", path=p)["id"]
    ok = S.connect(a, b, path=p)
    assert ok["connected"] is True
    # 不兼容：采集 -> ASR（profile_links 接 video_files 失败）
    c = S.add_node("asr_extract", path=p)["id"]
    bad = S.connect(a, c, path=p)
    assert bad["connected"] is False and "契约" in bad["reason"]

def test_set_and_delete(tmp_path):
    p = tmp_path / "active.json"
    a = S.add_node("douyin_profile_collect", path=p)["id"]
    S.set_params(a, {"主页链接": "http://x"}, path=p)
    wf = S.load_workflow(p)
    assert wf["nodes"][0]["params"]["主页链接"] == "http://x"
    S.delete_node(a, path=p)
    assert S.load_workflow(p)["nodes"] == []
```

- [ ] **Step 2: 跑测试确认失败**。

- [ ] **Step 3: 实现编排操作**（追加到 store.py）。要点：
  - `add_node(type, params=None, x=120, y=320, path=None)`：校验 type∈NODE_SPEC_BY_TYPE；
    生成 `node_<uuid8>` id；params 缺省取 spec.default_params；append 后 save；返回 `{"id":...}`。
  - `connect(source_id, target_id, path=None)`：取两节点 spec，
    `from app.manifest import best_connection, describe_port`；不兼容返回
    `{"connected": False, "reason": f"数据契约不匹配：... "}`；兼容则 append edge `{id,source_id,target_id}` 后 save。
  - `set_params(node_id, params, path)`：更新该节点 params（dict.update）后 save。
  - `delete_node(node_id, path)`：移除节点及相连 edge 后 save。
  - 复用 `app/models.py` 的 id 生成风格（`f"node_{uuid4().hex[:8]}"` / `f"edge_..."`）。

- [ ] **Step 4: 跑测试确认通过**。
- [ ] **Step 5: （可选）暂存**。

---

## Task 4: store 运行 + 确认闸门（接 WorkflowRunner）

**Files:** Modify `app/mcp/store.py`；Test 追加

- [ ] **Step 1: 追加失败测试**

```python
def test_run_all_mock_and_writes_back(tmp_path):
    p = tmp_path / "active.json"
    a = S.add_node("douyin_profile_collect", path=p)["id"]
    b = S.add_node("douyin_video_download", path=p)["id"]
    S.connect(a, b, path=p)
    res = S.run(path=p)  # run_all，默认模拟
    assert a in res["ran"] and b in res["ran"]
    wf = S.load_workflow(p)
    statuses = {n["id"]: n["status"] for n in wf["nodes"]}
    assert statuses[a] == "success" and statuses[b] == "success"

def test_run_real_high_risk_needs_confirmation(tmp_path):
    p = tmp_path / "active.json"
    a = S.add_node("mediapush_publish", params={"执行模式": "真实"}, path=p)["id"]
    res = S.run(path=p)
    assert res["needs_confirmation"] and res["needs_confirmation"][0]["id"] == a
```

- [ ] **Step 2: 跑测试确认失败**。

- [ ] **Step 3: 实现 `run`**（store.py）。要点：
  - 读 active.json，把每个 node dict 反序列化成 `WorkflowNode`
    （`spec=NODE_SPEC_BY_TYPE[type]`，回填 id/params/status/last_output；
    edges 反序列化成 `WorkflowEdge`）。
  - `WorkflowRunner().run_sync(nodes, edges, start_ids, confirm)`。
  - 把每个 node 的 status/last_output/error 写回对应 node dict，save（rev+1）。
  - 返回 runner 的 summary。
  - `run_node(node_id, confirm)` = `run(start_ids=[node_id])` 但只跑该单节点
    （传 start_ids=[node_id] 且 scope 限定为自身——单节点用 `start_ids=[id]` 且不向下扩展时，
    提供 `single=True` 参数让 scope 只含自身）。简单做法：`run` 接 `start_ids` 与
    `cascade: bool=True`；run_node 传 cascade=False（scope 只含该节点）。

- [ ] **Step 4: 跑测试确认通过**。
- [ ] **Step 5: （可选）暂存**。

---

## Task 5: store 造节点（预览/落盘）

**Files:** Modify `app/mcp/store.py`；Test 追加

- [ ] **Step 1: 追加失败测试**

```python
def test_create_skill_node_preview_then_write(tmp_path, monkeypatch=None):
    # 用一个临时 SKILL.md
    skill = tmp_path / "SKILL.md"
    skill.write_text("---\nname: demo\ndescription: d\n---\n正文", encoding="utf-8")
    preview = S.create_skill_node(str(skill), mode="text", write=False)
    assert preview["manifest"]["type"].startswith("skill_") and preview["written"] is False
```

（落盘分支会写 custom_nodes.json + reload，建议在测试里只验证 write=False 预览，
write=True 的副作用留给手动/集成验证，避免污染节点库。）

- [ ] **Step 2: 跑测试确认失败**。

- [ ] **Step 3: 实现 `create_skill_node`**（store.py）：调
  `app.agent.node_builder.build_node_manifest`；`write=False` 返回 `{"manifest":..., "written": False}`；
  `write=True` 调 `write_custom_node` + `app.models.reload_node_specs`，返回 `{"manifest":..., "written": True}`。
  另加 `list_nodes()` = `app.agent.planner.node_protocol_prompt()`（返回字符串）；
  `get_output(node_id, path)`、`get_logs(limit, path)`（读 runs 最新日志尾部）。

- [ ] **Step 4: 跑测试确认通过**。
- [ ] **Step 5: （可选）暂存**。

---

## Task 6: MCP server 薄壳（FastMCP stdio）

**Files:** Create `app/mcp/server.py`、`.mcp.json`

- [ ] **Step 1: 装依赖**：`uv pip install mcp`（工作台目录）。验证：
  `& $py -c "import mcp; print('ok')"`（用 venv 或装了 mcp 的解释器）。

- [ ] **Step 2: 写 server.py**（薄壳，每个工具调 store）：

```python
"""workbench MCP server（stdio）。工具只做参数校验 + 调 app.mcp.store。"""
from __future__ import annotations
from mcp.server.fastmcp import FastMCP
from app.mcp import store as S

mcp = FastMCP("video-workbench")

@mcp.tool()
def list_nodes() -> str:
    """列出全部可用节点类型及其能力/端口/参数（厚 Manifest JSON）。"""
    return S.list_nodes()

@mcp.tool()
def get_workflow() -> dict:
    """当前画布：节点/连线/状态/产物。"""
    return S.load_workflow()

@mcp.tool()
def add_node(type: str, params: dict | None = None, x: int = 120, y: int = 320) -> dict:
    return S.add_node(type, params=params, x=x, y=y)

@mcp.tool()
def connect(source_id: str, target_id: str) -> dict:
    return S.connect(source_id, target_id)

@mcp.tool()
def set_params(node_id: str, params: dict) -> dict:
    return S.set_params(node_id, params)

@mcp.tool()
def delete_node(node_id: str) -> dict:
    return S.delete_node(node_id)

@mcp.tool()
def run_node(node_id: str, confirm: bool = False) -> dict:
    return S.run(start_ids=[node_id], cascade=False, confirm=confirm)

@mcp.tool()
def run_chain(node_id: str, confirm: bool = False) -> dict:
    return S.run(start_ids=[node_id], cascade=True, confirm=confirm)

@mcp.tool()
def run_all(confirm: bool = False) -> dict:
    return S.run(confirm=confirm)

@mcp.tool()
def get_output(node_id: str) -> dict:
    return S.get_output(node_id)

@mcp.tool()
def get_logs(limit: int = 50) -> str:
    return S.get_logs(limit)

@mcp.tool()
def create_skill_node(skill_file: str, mode: str = "text", instruction: str = "", write: bool = False) -> dict:
    return S.create_skill_node(skill_file, mode=mode, instruction=instruction, write=write)

@mcp.tool()
def reload_nodes() -> dict:
    from app.models import reload_node_specs
    reload_node_specs(); return {"reloaded": True}

def main() -> None:
    mcp.run()  # stdio

if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 写 `.mcp.json`**（Claude Code 接入；用真实解释器或装了 mcp 的 venv）：

```json
{
  "mcpServers": {
    "video-workbench": {
      "command": "C:\\Users\\ASUS\\AppData\\Local\\Programs\\Python\\Python313\\python.exe",
      "args": ["-m", "app.mcp.server"],
      "cwd": "J:\\MagicTool\\个人网站\\tools\\短视频节点画布工作台"
    }
  }
}
```

- [ ] **Step 4: server 导入冒烟**：`& $py -c "import app.mcp.server"`（装了 mcp 的解释器），
  Expected: 无异常。工具逻辑已被 store 单测覆盖，server 仅验证可导入/注册。

- [ ] **Step 5: （可选）暂存**。

---

## Task 7: GUI 改走 Runner（engine.py 适配）

**Files:** Modify `app/runtime/engine.py`、`app/ui/main_window.py`

- [ ] **Step 1: engine.py 改为 Runner 适配**：保留 `RuntimeEngine(QObject)` 与
  `node_started/node_finished/node_failed/log` 信号；内部用 `WorkflowRunner`，
  `on_progress` 回调里 emit 对应 Qt 信号。提供 `run_workflow(nodes, edges, start_ids, confirm)`
  在后台线程跑 `run_sync`（真实节点耗时），通过信号回主线程更新。
  - 保留 `run_node(node, upstream_outputs)` 兼容签名（内部用单节点 run_sync）。

- [ ] **Step 2: main_window 跑流程改走 engine.run_workflow**：`_run_chain/_start_ready_nodes`
  简化为构造 scope 后调 `self.engine.run_workflow(self.scene.nodes, self.scene.edges, start_ids)`；
  节点状态经信号刷新（沿用 `_refresh_node`）。`_confirm_node_run` 的确认仍在 GUI 侧
  （真实高风险弹框；确认后把这些 id 作为 confirm 传给 run_workflow）。

- [ ] **Step 3: GUI 冒烟（offscreen, venv）**：构造 MainWindow，
  `add_node` 三个抖音节点连链，调跑流程（模拟），断言三节点 status 变 success。
  Expected: 通过（行为与改造前一致）。

- [ ] **Step 4: 全套测试**确保 engine 改造无回归。
- [ ] **Step 5: （可选）暂存**。

---

## Task 8: GUI 实时同步（active.json autosave + 监听重绘）

**Files:** Modify `app/ui/main_window.py`

- [ ] **Step 1: autosave 到 active.json**：GUI 每次结构改动（加节点/连线/改参/删/跑完）
  把当前画布写入 `workflows/active.json`（复用 store.save_workflow 或等价序列化），
  记下 `self._last_rev`。启动时若 active.json 存在则加载它（替代 `_seed_demo`，或保留 demo 仅当空）。

- [ ] **Step 2: QFileSystemWatcher 监听**：`watcher = QFileSystemWatcher([str(ACTIVE_PATH)])`；
  `fileChanged` → 防抖（QTimer 150ms）→ 读文件 rev，若 `rev > self._last_rev`（外部=MCP 改的），
  重载场景（clear + 按 active.json 重建节点/连线/状态）并更新 `_last_rev`。
  注意：写文件后部分平台 watcher 需重新 addPath；处理 removePath/addPath。

- [ ] **Step 3: GUI 冒烟（offscreen, venv）**：
  - 构造 MainWindow（指向临时 active.json）。
  - 用 `app.mcp.store.add_node(..., path=临时)` 模拟"MCP 改了文件"（rev 自增）。
  - 手动触发 reload（或调监听回调），断言画布新增了该节点。
  - 反向：GUI 自身 autosave 一次，断言不会触发重载回环（rev 比对）。

- [ ] **Step 4: （可选）暂存**。

---

## Task 9: 接入文档 + 全量验证

- [ ] **Step 1: 全套 runner**：遍历 `tests` 全部 `test_*`，Expected 全绿（新增 runner/store 测试）。
- [ ] **Step 2: server 导入冒烟 + GUI 冒烟**（offscreen）全过。
- [ ] **Step 3: 端到端手动校验脚本**（headless，不起真实 MCP）：用 store 直接
  add→connect→run 串"文章配图→文生图（模拟）"，确认 active.json 状态写回；
  再用 GUI offscreen 加载该 active.json 断言能重建。
- [ ] **Step 4: 文档**：README/PROJECT_PROGRESS 增"MCP 控制面"小节 +
  Claude Code 接入步骤（`claude mcp add` 或 `.mcp.json`）；PRD §11 标"已实现"。
- [ ] **Step 5: 交回用户决定是否 commit**。

---

## 验收（对应 spec）

1. `.mcp.json` 就绪；server 可导入、`list_nodes` 返回厚 Manifest。
2. 经 store/MCP 工具搭链路、run、读产物/日志（store 单测 + 手动端到端覆盖）。
3. GUI 监听 active.json，MCP 改动后画布重建（offscreen 冒烟覆盖）。
4. 高风险真实节点不带 confirm 不执行（runner + store 单测覆盖）。
5. create_skill_node 预览/落盘可用。
6. WorkflowRunner/store 单测通过；GUI 与 MCP 共用 Runner。
