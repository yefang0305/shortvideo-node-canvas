# 移除内置总控 + 统一凭证配置 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 拿掉工作台内置的总控/计划器角色，并把所有 API 密钥收拢到一个统一凭证文件，节点按名引用。

**Architecture:** 删除计划器 UI 与代码（保留厚 Manifest、造节点后端、诊断等基础设施）；新增 `config/credentials.json` + `app/runtime/credentials.py`，`AgentSettings` 兼容层改读凭证文件的 `model` 段；skill 节点经新占位符 `{cred:服务.字段}` 注入服务商密钥；参数面板对引用凭证的节点显示只读"口子"提示。

**Tech Stack:** Python 3.13、PyQt5、JSON 配置、既有 skill_node 执行器。

**Spec:** docs/superpowers/specs/2026-05-31-移除内置总控与统一凭证-design.md

---

## 环境与约定（本机特例）

- **真实解释器**：`C:\Users\ASUS\AppData\Local\Programs\Python\Python313\python.exe`（`python` 是坏 stub，勿用）。
- **无 pytest**：用临时 runner 调 `test_*` 函数。单测运行模板（PowerShell）：

```powershell
$py = "C:\Users\ASUS\AppData\Local\Programs\Python\Python313\python.exe"; $env:PYTHONIOENCODING="utf-8"; $proj = "J:\MagicTool\个人网站\tools\短视频节点画布工作台"
& $py -X utf8 -c "import sys,os,inspect,tempfile,pathlib; sys.path.insert(0,r'$proj'); os.chdir(r'$proj'); import tests.<MOD> as m; f=getattr(m,'<FN>'); (f(pathlib.Path(tempfile.mkdtemp())) if 'tmp_path' in inspect.signature(f).parameters else f()); print('PASS <FN>')"
```

- 全套 runner（跑全部 `test_*`）：见既有会话用法（遍历 `tests` 包，tmp_path 注入临时目录）。
- **GUI 冒烟**用带 PyQt5 的 venv：`J:\MagicTool\Standalone\VideoCut\video_tool\venv\Scripts\python.exe` + `$env:QT_QPA_PLATFORM="offscreen"`。
- **提交**：遵守用户全局规则——**只在用户要求时 commit**。下方每个 Task 的 commit 步骤为“可选/批量”，执行期默认 staged 不提交，等用户统一确认。

## File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `app/runtime/credentials.py` | 新建 | 凭证文件读写 + `{cred:}` 取值 + 旧 agent_settings 迁移 |
| `config/credentials.example.json` | 新建 | 凭证文件模板（进 git，真实文件 gitignore）|
| `.gitignore` | 改 | 加 `config/credentials.json` |
| `app/agent/settings.py` | 改 | `load/save_agent_settings` 委托到 credentials 的 `model` 段 |
| `app/runtime/tool_executor.py` | 改 | `_subst_token` 增加 `{cred:服务.字段}` 解析 |
| `app/agent/planner.py` | 改 | 删除 `SimpleAgentPlanner` 等，仅保留 `node_protocol_prompt`/`_port_manifest` |
| `app/agent/llm_client.py` | 改 | `OpenAICompatiblePlanner`→`LLMDiagnoser`，仅保留 `diagnose_error` |
| `app/ui/main_window.py` | 改 | 删计划器/造节点 UI；总控 tab→设置 tab；参数面板加凭证口子提示 |
| `app/nodes/custom_nodes.json` | 改 | 文生图节点去裸 key，env 改 `{cred:}` |
| `tests/test_credentials.py` | 新建 | 凭证读写/迁移/`{cred:}`/兼容层 |
| `tests/test_skill_script.py` | 改 | 文生图 env 断言改凭证引用 |
| `tests/test_planner.py` | 删除 | 计划器已移除 |

---

## Task 1: 凭证模块 credentials.py

**Files:**
- Create: `app/runtime/credentials.py`
- Test: `tests/test_credentials.py`

- [ ] **Step 1: 写失败测试** （`tests/test_credentials.py`）

```python
"""统一凭证文件读写、{cred:} 取值、旧 agent_settings 迁移。"""
from __future__ import annotations
import json
from pathlib import Path
from app.runtime import credentials as C

def test_default_skeleton_when_missing(tmp_path):
    data = C.load_credentials(tmp_path / "credentials.json")
    assert "model" in data and "services" in data

def test_save_then_load_roundtrip(tmp_path):
    p = tmp_path / "credentials.json"
    C.save_credentials({"model": {"api_key": "k"}, "services": {"openrouter": {"api_key": "sk"}}}, p)
    data = C.load_credentials(p)
    assert data["services"]["openrouter"]["api_key"] == "sk"

def test_get_credential_value(tmp_path):
    p = tmp_path / "credentials.json"
    C.save_credentials({"model": {}, "services": {"openrouter": {"api_key": "sk-x"}}}, p)
    assert C.get_credential_value("openrouter.api_key", p) == "sk-x"
    assert C.get_credential_value("missing.api_key", p) == ""
    assert C.get_credential_value("openrouter.nope", p) == ""

def test_migrates_legacy_agent_settings(tmp_path):
    (tmp_path / "agent_settings.json").write_text(
        json.dumps({"api_base": "B", "api_key": "K", "model": "M"}), encoding="utf-8")
    data = C.load_credentials(tmp_path / "credentials.json", legacy_path=tmp_path / "agent_settings.json")
    assert data["model"] == {"api_base": "B", "api_key": "K", "model": "M"}
```

- [ ] **Step 2: 跑测试确认失败**（模块不存在）。用单测模板，MOD=`test_credentials`，逐个 FN。Expected: FAIL（ModuleNotFoundError）。

- [ ] **Step 3: 实现 credentials.py**

```python
"""统一凭证配置：模型 API + 各服务商密钥集中一处，节点按名引用。"""
from __future__ import annotations
import json
from pathlib import Path
from typing import Any

CONFIG_DIR = Path(__file__).resolve().parents[2] / "config"
CREDENTIALS_PATH = CONFIG_DIR / "credentials.json"
LEGACY_AGENT_SETTINGS = CONFIG_DIR / "agent_settings.json"


def _default() -> dict[str, Any]:
    return {"model": {"api_base": "", "api_key": "", "model": ""}, "services": {}}


def load_credentials(path: Path = CREDENTIALS_PATH, legacy_path: Path = LEGACY_AGENT_SETTINGS) -> dict[str, Any]:
    if path.exists():
        data = json.loads(path.read_text(encoding="utf-8"))
    else:
        data = _default()
        if legacy_path and legacy_path.exists():
            try:
                legacy = json.loads(legacy_path.read_text(encoding="utf-8"))
                data["model"] = {
                    "api_base": legacy.get("api_base", ""),
                    "api_key": legacy.get("api_key", ""),
                    "model": legacy.get("model", ""),
                }
            except (json.JSONDecodeError, OSError):
                pass
    data.setdefault("model", {})
    data.setdefault("services", {})
    return data


def save_credentials(data: dict[str, Any], path: Path = CREDENTIALS_PATH) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")


def get_service(name: str, path: Path = CREDENTIALS_PATH) -> dict[str, Any]:
    return dict(load_credentials(path).get("services", {}).get(name, {}))


def get_credential_value(ref: str, path: Path = CREDENTIALS_PATH) -> str:
    """ref 形如 'openrouter.api_key'。缺失返回空串。"""
    if "." not in ref:
        return ""
    name, field = ref.split(".", 1)
    return str(get_service(name, path).get(field, "") or "")
```

- [ ] **Step 4: 跑测试确认通过**（4 个 test 全 PASS）。
- [ ] **Step 5: （可选）暂存** `git add app/runtime/credentials.py tests/test_credentials.py`。

---

## Task 2: 执行器支持 {cred:} 占位

**Files:**
- Modify: `app/runtime/tool_executor.py`（`_subst_token`）
- Test: `tests/test_credentials.py`（追加）

- [ ] **Step 1: 追加失败测试**

```python
def test_subst_token_resolves_cred(tmp_path, monkeypatch=None):
    import app.runtime.credentials as C
    from app.runtime import tool_executor as T
    p = tmp_path / "credentials.json"
    C.save_credentials({"model": {}, "services": {"openrouter": {"api_key": "sk-9"}}}, p)
    # 让执行器用临时凭证文件
    orig = C.CREDENTIALS_PATH
    C.CREDENTIALS_PATH = p
    try:
        val, is_ph = T._subst_token("{cred:openrouter.api_key}", {}, "")
        assert (val, is_ph) == ("sk-9", True)
        env = T._resolve_env({"OPENROUTER_API_KEY": "{cred:openrouter.api_key}", "X": "{cred:none.x}"}, {})
        assert env == {"OPENROUTER_API_KEY": "sk-9"}
    finally:
        C.CREDENTIALS_PATH = orig
```

- [ ] **Step 2: 跑测试确认失败**（`{cred:}` 未识别，返回原样、非占位）。

- [ ] **Step 3: 在 `_subst_token` 增加分支**（`app/runtime/tool_executor.py`），位于 `{param:}` 分支之后、`return tok, False` 之前：

```python
    if tok.startswith("{cred:") and tok.endswith("}"):
        from app.runtime.credentials import get_credential_value
        return get_credential_value(tok[len("{cred:"):-1]), True
```

- [ ] **Step 4: 跑测试确认通过**。
- [ ] **Step 5: （可选）暂存**。

---

## Task 3: AgentSettings 委托到凭证文件 model 段

**Files:**
- Modify: `app/agent/settings.py`
- Test: `tests/test_credentials.py`（追加）

- [ ] **Step 1: 先读** `app/agent/settings.py` 现有内容，记下默认 base/model 常量与 `AgentSettings` 字段。

- [ ] **Step 2: 追加失败测试**

```python
def test_load_agent_settings_reads_model_section(tmp_path):
    import app.runtime.credentials as C
    from app.agent import settings as S
    p = tmp_path / "credentials.json"
    C.save_credentials({"model": {"api_base": "B", "api_key": "K", "model": "M"}, "services": {}}, p)
    orig = C.CREDENTIALS_PATH; C.CREDENTIALS_PATH = p
    try:
        s = S.load_agent_settings()
        assert (s.api_base, s.api_key, s.model) == ("B", "K", "M")
    finally:
        C.CREDENTIALS_PATH = orig
```

- [ ] **Step 3: 改 settings.py**：保留 `AgentSettings` 数据类与默认值常量，把 `load_agent_settings/save_agent_settings` 改为委托：

```python
from app.runtime.credentials import load_credentials, save_credentials

def load_agent_settings() -> AgentSettings:
    m = load_credentials().get("model", {})
    return AgentSettings(
        api_base=(m.get("api_base") or DEFAULT_API_BASE),
        api_key=(m.get("api_key") or ""),
        model=(m.get("model") or DEFAULT_MODEL),
    )

def save_agent_settings(settings: AgentSettings) -> None:
    data = load_credentials()
    data["model"] = {"api_base": settings.api_base, "api_key": settings.api_key, "model": settings.model}
    save_credentials(data)
```
（`DEFAULT_API_BASE`/`DEFAULT_MODEL` 用 settings.py 里现有的默认值常量；若原来是字面量，提取为常量。）

- [ ] **Step 4: 跑测试确认通过**；并跑既有 `test_skill_script.py` 中不涉网用例确保 import 无回归。
- [ ] **Step 5: （可选）暂存**。

---

## Task 4: 文生图节点迁移到凭证引用

**Files:**
- Modify: `app/nodes/custom_nodes.json`（type=`skill_baoyu_image_gen`）
- Modify: `tests/test_skill_script.py`（`test_image_gen_node_is_registered`）

- [ ] **Step 1: 改测试断言**（`tests/test_skill_script.py`）：

```python
def test_image_gen_node_is_registered():
    from app.models import load_node_specs
    node = {s.type: s for s in load_node_specs()}.get("skill_baoyu_image_gen")
    assert node is not None
    b = node.skill_binding
    assert b["mode"] == "script" and b["script"].endswith("main.ts")
    assert any("{output}" in a for a in b["args"])
    # 凭证引用，不再有裸 key 参数
    assert b["env"]["OPENROUTER_API_KEY"] == "{cred:openrouter.api_key}"
    assert "OPENROUTER_API_KEY" not in node.default_params
```

- [ ] **Step 2: 跑测试确认失败**（当前 env 是 `{param:...}`、default_params 含裸 key）。

- [ ] **Step 3: 改 custom_nodes.json 的文生图节点**：
  - `default_params` 删除 `OPENROUTER_API_KEY/OPENAI_API_KEY/DASHSCOPE_API_KEY/REPLICATE_API_TOKEN`，保留 `执行模式/服务商/比例/质量`。
  - `skill_binding.env` 改为：
    ```json
    "env": {
      "OPENROUTER_API_KEY": "{cred:openrouter.api_key}",
      "OPENAI_API_KEY": "{cred:openai.api_key}",
      "DASHSCOPE_API_KEY": "{cred:dashscope.api_key}",
      "REPLICATE_API_TOKEN": "{cred:replicate.api_token}"
    }
    ```

- [ ] **Step 4: 跑测试确认通过**。
- [ ] **Step 5: （可选）暂存**。

---

## Task 5: 删除总控大脑（planner / llm_client / test_planner）

**Files:**
- Modify: `app/agent/planner.py`
- Modify: `app/agent/llm_client.py`
- Delete: `tests/test_planner.py`

- [ ] **Step 1: 删 `tests/test_planner.py`**（`git rm tests/test_planner.py` 或直接删除文件）。

- [ ] **Step 2: 精简 planner.py**：删除 `SimpleAgentPlanner`、`AgentPlan`、`PlannedNode`、`PlannedEdge`、`parse_plan_json`、`_match_specs`、`_order_and_link`、`_spec_triggers`；移除 `from app.manifest import best_connection` 导入（若仅这些用到）。**保留** `_port_manifest`、`node_protocol_prompt`。保留 `import json`、`NODE_SPECS`（node_protocol_prompt 用）；删 `re`、`dataclass/field`、`NODE_SPEC_BY_TYPE` 等仅被删代码用到的导入。

- [ ] **Step 3: 改 llm_client.py**：把 `OpenAICompatiblePlanner` 改名 `LLMDiagnoser`，删除 `plan` 方法与对 `node_protocol_prompt/parse_plan_json/AgentPlan` 的导入；保留 `diagnose_error` 与 `LLMClientError`、`AgentSettings` 导入。

- [ ] **Step 4: 跑保留测试验证无回归**：`test_manifest.py`（含 `node_protocol_prompt`）、`test_node_builder.py`、`test_skill_script.py`、`test_credentials.py` 全绿。Expected: PASS（test_planner 已删，不再收集）。

- [ ] **Step 5: （可选）暂存**（含删除）。

---

## Task 6: UI——删计划器/造节点，总控 tab 改设置 tab，加凭证口子

**Files:**
- Modify: `app/ui/main_window.py`

- [ ] **Step 1: 改导入**：删除 `SimpleAgentPlanner`、`AgentPlan`、`build_node_manifest`、`write_custom_node`、`OpenAICompatiblePlanner`；改为导入 `LLMDiagnoser`。`reload_node_specs` 不再被 UI 用（造节点删了）→ 若无其它用处可一并删导入。

- [ ] **Step 2: `__init__` 清理**：删 `self.agent = SimpleAgentPlanner()`、`self.pending_plan`、`self.pending_manifest`。

- [ ] **Step 3: 改 `_build_agent_tab`**：
  - tab 标题从「总控」改「设置」（`self.tabs.addTab(self.agent_tab, "设置")`）。
  - 删除：`agent_input`、`plan_btn`、`apply_plan_btn`、`_build_node_factory()` 调用与该方法及其 handler（`_pick_skill_file/_preview_skill_node/_confirm_skill_node`）。
  - 保留：API 配置表单（base/model/key）、`agent_output`（复用为诊断输出区，标题改「失败诊断」）。
  - 在 API 配置区加一行说明 Label：`其它服务密钥在 config/credentials.json 配置`。

- [ ] **Step 4: 改 `_connect_signals`**：删除 `plan_btn/apply_plan_btn/preview_node_btn/confirm_node_btn` 的连接；保留 `diagnose_btn`。

- [ ] **Step 5: 删 handler**：`_generate_agent_plan`、`_apply_agent_plan`、`_build_node_factory`、`_pick_skill_file`、`_preview_skill_node`、`_confirm_skill_node`。`_diagnose_current_error` 改用 `LLMDiagnoser`，输出仍写 `agent_output`、切到设置 tab。

- [ ] **Step 6: 参数面板加凭证口子**：在 `_show_node_params` 列完参数后，若 `node.spec.skill_binding`，扫描 `env`/`args` 里的 `{cred:NAME.field}`，去重后对每个 NAME 追加只读行：

```python
binding = node.spec.skill_binding or {}
refs = []
for v in list((binding.get("env") or {}).values()) + list(binding.get("args") or []):
    s = str(v)
    if s.startswith("{cred:") and s.endswith("}"):
        refs.append(s[len("{cred:"):-1].split(".")[0])
for name in dict.fromkeys(refs):
    self.param_form.addRow(QLabel("需要凭证"), QLabel(f"{name}（在 config/credentials.json 配置）"))
```

- [ ] **Step 7: GUI 冒烟（offscreen，venv）**：构造 `MainWindow`；断言 tab 标题含「设置」、不含「总控」；选中 `skill_baoyu_image_gen` 后 `_show_node_params` 不抛错且含"需要凭证"行；选中纯本地节点不含该行。Expected: 全过。

- [ ] **Step 8: （可选）暂存**。

---

## Task 7: gitignore + 凭证模板

**Files:**
- Create: `config/credentials.example.json`
- Modify: `.gitignore`

- [ ] **Step 1: 写 `config/credentials.example.json`**（结构同 spec，值留空）。
- [ ] **Step 2: `.gitignore` 追加** `config/credentials.json`。确认 `agent_settings.json` 仍在 ignore（保持）。
- [ ] **Step 3: （可选）暂存**。

---

## Task 8: 全量验证 + 收尾

- [ ] **Step 1: 跑全套 runner**：遍历 `tests` 全部 `test_*`，Expected: 全绿（无 test_planner）。
- [ ] **Step 2: GUI 冒烟**：`MainWindow` 能构造、无总控/造节点入口、设置 tab 在、诊断按钮在。
- [ ] **Step 3: 手动核对 spec 验收 1–5 项**逐条满足。
- [ ] **Step 4: 更新文档**：`PROJECT_PROGRESS.md` 标注"内置总控已移除/统一凭证已落地"；`README` 若提到总控用法做对应删改（最小改动）。
- [ ] **Step 5: 交回用户决定是否 commit**（遵守只在用户要求时提交）。

---

## 验收（对应 spec）

1. 设置 tab 可配模型 API 并存入 credentials.json；无计划器/造节点入口。
2. 文生图节点无裸 key；配 `services.openrouter.api_key` 后 `{cred:}` 注入通过。
3. 文生图选中显示"需要凭证"提示；纯本地节点不显示。
4. 失败诊断可用。
5. 全套测试通过（新增凭证测试，移除计划器测试）。
